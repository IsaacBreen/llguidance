name: Temporary Real CFA Speculative Branch Benchmark

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  bench:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary benchmark disabled; see real-cfa-spec-benchmark-results-2026-08-11.md'
